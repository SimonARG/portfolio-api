includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - app
        - database
        - routes

    # database/data holds harvested content, not code.
    excludePaths:
        - database/data
